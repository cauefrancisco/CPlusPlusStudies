#include <iostream>
#include <cmath>

int main()
{
  double x = 3;
  double y = 4;
  double z;

  // z = std::max(x, y);
  // z = std::min(x, y);
  // z = pow(x, y);  x to the power of y
  // z = sqrt(9) Squared root
  // z = abs(-3) absolute value
  // z = round(x) round to the closest up/down
  // z = ceil(x) round up.
  // z = floor(x) round down.

  std::cout << z;

  return 0;
}